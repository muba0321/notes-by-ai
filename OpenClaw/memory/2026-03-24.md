### [PROJECT: OpenClaw 新机器部署] Gateway 持续崩溃问题

**结论** → 新机器 (154.193.217.90) OpenClaw Gateway 不断崩溃重启，每次运行 5-15 秒后收到 SIGTERM 信号

**变更** → 无（问题待解决）

**教训** → 
- systemd 服务配置可能有问题，需要检查 service 文件
- 每次都是 SIGTERM 信号（systemd 发送），不是进程崩溃
- journalctl --user 没有日志，service 配置可能异常
- 修改 bind 从 lan 到 loopback 不能解决问题

**问题模式：**
```
Gateway 启动 → 运行 5-15 秒 → 收到 SIGTERM → 关闭 → 自动重启（循环）
```

**可能原因：**
1. systemd service 配置问题（最可能）
2. OpenClaw 自身重启机制触发
3. Node.js v22.22.1 兼容性问题
4. openclaw.json 配置冲突

**待确认：**
- systemd service 文件内容
- 手动启动是否稳定
- dmesg 系统日志
- OOM killer 是否触发

**当前状态：** ⏸️ 等待用户提供更多信息

---
